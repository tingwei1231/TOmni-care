# ===== TOmni-Care Agent — 第一階段依賴 =====
# 安裝方式：pip install -r requirements.txt
# Colab 補充：!pip install -r requirements.txt

# --- ASR ---
faster-whisper>=1.0.3          # Whisper CTranslate2 加速推論
silero-vad>=5.1.2              # 輕量 VAD（語音端點偵測）
soundfile>=0.12.1              # 音訊讀寫
numpy>=1.24.0

# --- TTS (Bert-VITS2 依賴) ---
torch>=2.1.0                   # PyTorch（GPU/CPU 皆可）
torchaudio>=2.1.0
transformers>=4.40.0           # HuggingFace Transformers（Bert 模型）
phonemizer>=3.2.1              # 音素轉換工具
jieba>=0.42.1                  # 中文分詞（漢字前處理）
pypinyin>=0.51.0               # 拼音輔助（用作台語音標對照）

# --- Gradio Demo ---
gradio>=4.31.0

# --- 工具 ---
librosa>=0.10.0                # 音訊特徵提取（第三階段情緒分析預備）
scipy>=1.11.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# --- 測試 ---
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ── 第二階段：LLM & RAG ─────────────────────────────────────
# LLM 後端（擇一或全裝）
groq>=0.11.0                   # Groq API（優先後端，速度最快）
openai>=1.50.0                 # OpenRouter 相容 OpenAI 格式
ollama>=0.3.0                  # Ollama 本地推論客戶端

# RAG 知識庫
chromadb>=0.5.0                # 向量資料庫（含持久化儲存）
sentence-transformers>=3.0.0   # Embedding + Cross-Encoder Reranking
pdfminer.six>=20221105         # PDF 文件解析
pypdf>=4.0.0                   # PDF 備用解析器
